# The demo does not read .env automatically and accepts no provider secrets.
# It runs with fictional fixtures and refuses COMPANY_OS_MODE values other than demo.
COMPANY_OS_MODE=demo
# Production needs an implemented OIDC flow, tenant configuration, workload identity,
# secret references, model gateway and scoped connectors. See docs/06-deployment.md.
